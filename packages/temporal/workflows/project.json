{
  "name": "workflows",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/temporal/workflows/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn tsc -b packages/temporal/workflows/tsconfig.json"
      },
      "outputs": [
        "{projectRoot}/dist"
      ]
    }
  }
}
