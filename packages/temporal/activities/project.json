{
  "name": "activities",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/temporal/activities/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn tsc -b packages/temporal/activities/tsconfig.json"
      },
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
